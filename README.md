this repo contains analysis. r that does the following:

reads the data
adds activity and subject identifiers
merges training and test data with meaningful column details
add activity labels 
summerizes values on activity labels and subjects 


